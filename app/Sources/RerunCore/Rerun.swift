/// Rerun — local, always-on screen memory for macOS.
/// This is the shared core library used by both the daemon and CLI.
public enum Rerun {
    public static let version = "0.2.1"
    public static let name = "Rerun"
}
